# dsh-colleague Cordis patch layer
# Auto-applied by the framework when installed to a DSH profile
- insert:
    - id: dsh-colleague
      name: dsh-colleague
      config:
        maxConcurrentWriters: 1
        memoryEnabled: true
