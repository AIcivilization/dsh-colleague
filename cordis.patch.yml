# colleague-plugin Cordis patch 层
# 安装到 DSH profile 时被框架自动 apply
- insert:
    - id: colleague-plugin
      name: colleague-plugin
      config:
        maxConcurrentWriters: 1
        memoryEnabled: true
