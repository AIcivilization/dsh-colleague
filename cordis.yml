# Colleague Plugin — DeepSeek Harness 插件配置
# 安装方式: dsh plugin --profile web add file:///path/to/colleague-plugin
# 启动方式: dsh --profile web web

- id: colleague-plugin
  name: 'colleague-plugin'
  config:
    maxConcurrentWriters: 1
    memoryEnabled: true
