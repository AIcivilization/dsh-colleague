# dsh-colleague — DeepSeek Harness plugin config
# Install: dsh plugin --profile web add file:///path/to/dsh-colleague
# Launch: dsh --profile web web

- id: dsh-colleague
  name: 'dsh-colleague'
  config:
    maxConcurrentWriters: 1
    memoryEnabled: true
